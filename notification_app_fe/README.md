# Notification App Frontend

React + Vite dashboard for notifications.

## Run

```bash
npm install
npm run dev
```

## Notes

- Uses the reusable logging middleware package
- Logs frontend lifecycle and API actions
- Proxies `/api` requests to the backend on port 3000
- Set `VITE_LOG_JWT_TOKEN` in `.env` for logging
