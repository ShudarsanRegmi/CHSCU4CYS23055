# Frontend .env.example (Vite)
# API base used by the frontend (proxy typically configured)
VITE_API_BASE_URL=http://localhost:3000

# Token for the logging middleware (exposed to client build)
VITE_LOG_JWT_TOKEN=your_jwt_token_here
